# api-security-tester

A tool that tests API endpoints for common vulnerabilities: broken authentication, rate limiting, injection, excessive data exposure.

Tech stack: Python, requests, JWT library, JSON
